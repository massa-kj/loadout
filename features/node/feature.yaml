spec_version: 1
mode: script
description: Node.js runtime with TypeScript and ESLint tooling
depends: []

requires:
  - name: runtime_manager

runtimes:
  # version resolved from profile config (LOADOUT_FEATURE_CONFIG_VERSION)
  - name: node
