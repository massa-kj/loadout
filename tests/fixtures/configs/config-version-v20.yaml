# Config with a versioned runtime (dummy-rt@20).
# Verifies: version is recorded in state.
# Used by: version_install scenario, version_upgrade scenario (first apply).

profile:
  components:
    local:
      dummy-rt:
        params:
          version: "20"

strategy:
  rules:
    - match:
        kind: package
      use: local/dummy-pkg
    - match:
        kind: runtime
      use: local/dummy-rt
  fs:
    backup: false
