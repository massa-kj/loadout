# Empty profile: removes all components.
# Used by: lifecycle, uninstall scenarios (full uninstall phase)

profile:
  components: {}

strategy:
  rules:
    - match:
        kind: package
      use: core/brew
    - match:
        kind: runtime
      use: core/mise
  fs:
    backup: false
