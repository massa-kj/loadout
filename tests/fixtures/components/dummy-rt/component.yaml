spec_version: 1
description: |
  Dummy runtime component for E2E testing.
  Uses the local/dummy-rt backend — no network access required.
  The version in this file is the default; the profile config can override it
  via the component's 'version' field.
depends: []

provides:
  - name: runtime_manager

resources:
  - kind: runtime
    id: rt:dummy-rt
    name: dummy-rt
    version: "1.0"
