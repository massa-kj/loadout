spec_version: 1
description: |
  Dummy package component for E2E testing.
  Uses the local/dummy-pkg backend — no network access required.
depends: []

provides:
  - name: package_manager

resources:
  - kind: package
    id: pkg:dummy-alpha
    name: dummy-alpha
