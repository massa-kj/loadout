[package]
name = "resolver"
version = "0.1.0"
edition = "2021"
description = "Dependency resolver for loadout"
publish = false

[dependencies]
model = { path = "../model" }
thiserror = "2"

[dev-dependencies]
serde_json = "1"
