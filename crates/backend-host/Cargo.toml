[package]
name = "backend-host"
version = "0.1.0"
edition = "2021"

[dependencies]
model = { path = "../model" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
serde_yaml = "0.9"
thiserror = "2"

[dev-dependencies]
tempfile = "3"
