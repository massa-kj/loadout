[package]
name = "feature-host"
version = "0.1.0"
edition = "2021"

[dependencies]
model = { path = "../model" }
platform = { path = "../platform" }
thiserror = "2"

[dev-dependencies]
tempfile = "3"
