[package]
name = "state"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
model = { path = "../model" }
io = { path = "../io" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "2"

[dev-dependencies]
tempfile = "3"
