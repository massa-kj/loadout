[package]
name = "config"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
model = { path = "../model" }
io = { path = "../io" }
thiserror = "2"

[dev-dependencies]
serde_json = "1"
tempfile = "3"
