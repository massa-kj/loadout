[package]
name = "model"
version = "0.1.0"
edition = "2021"
description = "Shared data types for loadout"
publish = false

[dependencies]
serde = { version = "1", features = ["derive"] }
platform = { path = "../platform" }

[dev-dependencies]
serde_json = "1"
